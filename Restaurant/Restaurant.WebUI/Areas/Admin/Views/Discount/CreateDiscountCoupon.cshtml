@using Restaurant.DtoLayer.DiscountCoupondtos
@model CreateDiscountCouponDto

@{
    ViewData["Title"] = "CreateDiscountCoupon";
    Layout = "~/Areas/Admin/Views/AdminUIControler/Index.cshtml";
}

<style>
    .switch {
        position: relative;
        display: inline-block;
        width: 52px;
        height: 26px;
        margin-left: 10px;
    }

        .switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

    .slider {
        position: absolute;
        cursor: pointer;
        background-color: #ccc;
        border-radius: 26px;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        transition: .3s;
    }

        .slider:before {
            position: absolute;
            content: "";
            height: 20px;
            width: 20px;
            left: 3px;
            bottom: 3px;
            background-color: white;
            border-radius: 50%;
            transition: .3s;
        }

    input:checked + .slider {
        background-color: #28a745;
    }

        input:checked + .slider:before {
            transform: translateX(26px);
        }

    .status-text {
        font-weight: 600;
        margin-left: 10px;
    }
</style>

<div class="row">
    <div class="col-md-3"></div>

    <div class="col-md-6">
        <div class="card">
            <form method="post" asp-action="CreateDiscountCoupon">
                <div class="card-body">
                    <h4 class="card-title mb-4">İndirim Kodu Güncelleme</h4>

                    <!-- Name -->
                    <div class="form-group row">
                        <label class="col-sm-4 text-right col-form-label">Kod İsmi</label>
                        <div class="col-sm-8">
                            <input asp-for="Code" class="form-control" />
                        </div>
                    </div>

                    <!-- Description -->
                    <div class="form-group row">
                        <label class="col-sm-4 text-right col-form-label">İndirim Oranı</label>
                        <div class="col-sm-8">
                            <input type="number" asp-for="Rate" class="form-control" />
                        </div>
                    </div>

                    <!-- DisplayOrder -->
                    <div class="form-group row">
                        <label class="col-sm-4 text-right col-form-label">Tarih</label>
                        <div class="col-sm-8">
                            <input asp-for="StartDate" type="date" class="form-control" />
                        </div>
                    </div>
                    <div class="form-group row">
                        <label class="col-sm-4 text-right col-form-label">Tarih</label>
                        <div class="col-sm-8">
                            <input asp-for="EndDate" type="date" class="form-control" />
                        </div>
                    </div>

                    <!-- IsActive Switch -->
                    <div class="form-group row align-items-center">
                        <label class="col-sm-4 text-right col-form-label">
                            Kategoride Gösterilsin mi?
                        </label>

                        <div class="col-sm-8 d-flex align-items-center">
                            <label class="switch mb-0">
                                <input type="checkbox" id="isActiveCheckbox" @(Model.IsActive ? "checked" : "") />
                                <span class="slider"></span>
                            </label>

                            <span id="statusText" class="status-text">
                                @(Model.IsActive ? "Aktif" : "Pasif")
                            </span>

                            <!-- GERÇEK DEĞERİ TAŞIYAN INPUT -->
                            <input type="hidden"
                                   name="IsActive"
                                   id="isActiveHidden"
                                   value="@(Model.IsActive.ToString().ToLower())" />
                        </div>
                    </div>
                </div>

                <div class="border-top">
                    <div class="card-body text-right">
                        <button type="submit" class="btn btn-primary">
                            Kaydet
                        </button>
                    </div>
                </div>

            </form>
        </div>
    </div>

    <div class="col-md-3"></div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const checkbox = document.getElementById("isActiveCheckbox");
        const hidden = document.getElementById("isActiveHidden");
        const text = document.getElementById("statusText");

        checkbox.addEventListener("change", function () {
            hidden.value = this.checked ? "true" : "false";
            text.innerText = this.checked ? "Aktif" : "Pasif";
        });
    });
</script>


